#import "./fragments.gql"

query GetUsers {
  users {
    ...UserInfo
  }
}

query GetPost($id: String!) {
  post(where: { id: $id }) {
    ...PostDetail
  }
}

query GetPosts {
  posts {
    ...PostListItem
  }
}
